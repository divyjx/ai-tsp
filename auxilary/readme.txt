Steps to run the program :
    1. download the zip file and extract it.
    2. run the "run.sh" file in terminal -->> "sh run.sh euc_100"
    3. repeat above for remaining cases

Note: 
    1. Tour and cost is printed in each iteration.
    2. Algorithm takes 300s to run but if time exceeds then we may have to manually terminate it.